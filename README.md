To copy environment run: conda env create -f environment.yml

Download dataset here: https://rpg.ifi.uzh.ch/E2DEPTH.html
- I recommend storing training sequences in folder called "train" (and similarly for validation and test sequences)
